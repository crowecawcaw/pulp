; openapi-typescript declares a peer of typescript@^5.x, but this project runs
; typescript 6.x. The tool only uses TS at runtime to emit types and works fine
; on TS 6, so we relax peer resolution rather than pin an old TypeScript.
legacy-peer-deps=true
